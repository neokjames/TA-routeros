<dashboard version="1.1">
  <label>RouterOS DNS Activity</label>
  <description>Overview of DNS queries and responses from Mikrotik RouterOS devices.</description>

  <fieldset submitButton="false" autoRun="true">
    <input type="time" token="time">
      <label>Time Range</label>
      <default>
        <earliest>-24h@h</earliest>
        <latest>now</latest>
      </default>
    </input>
    <input type="text" token="index_filter" searchWhenChanged="true">
      <label>Index</label>
      <default>network</default>
    </input>
    <input type="text" token="host_filter" searchWhenChanged="true">
      <label>Host filter (wildcards OK)</label>
      <default>*</default>
    </input>
  </fieldset>

  <row>
    <panel>
      <title>Total Queries</title>
      <single>
        <search>
          <query>index=$index_filter$ sourcetype=routeros eventtype=routeros_dns_query host=$host_filter$ | stats count</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
        <option name="drilldown">none</option>
      </single>
    </panel>
    <panel>
      <title>Unique Clients</title>
      <single>
        <search>
          <query>index=$index_filter$ sourcetype=routeros eventtype=routeros_dns_query host=$host_filter$ | stats dc(src)</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
        <option name="drilldown">none</option>
      </single>
    </panel>
    <panel>
      <title>Unique Domains</title>
      <single>
        <search>
          <query>index=$index_filter$ sourcetype=routeros eventtype=routeros_dns_query host=$host_filter$ | stats dc(query)</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
        <option name="drilldown">none</option>
      </single>
    </panel>
    <panel>
      <title>NXDOMAIN Responses</title>
      <single>
        <search>
          <query>index=$index_filter$ sourcetype=routeros eventtype=routeros_dns_response host=$host_filter$ reply_code=NXDomain | stats count</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
        <option name="drilldown">none</option>
        <option name="useColors">1</option>
        <option name="rangeColors">["0x53a051","0xf8be34","0xf1813f","0xdc4e41"]</option>
        <option name="rangeValues">[100,1000,10000]</option>
      </single>
    </panel>
  </row>

  <row>
    <panel>
      <title>Queries Over Time by Record Type</title>
      <chart>
        <search>
          <query>index=$index_filter$ sourcetype=routeros eventtype=routeros_dns_query host=$host_filter$ | timechart count by record_type useother=f</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
        <option name="charting.chart">column</option>
        <option name="charting.chart.stackMode">stacked</option>
        <option name="charting.legend.placement">right</option>
      </chart>
    </panel>
    <panel>
      <title>DNS Response Latency</title>
      <description>Average and 95th-percentile query-to-response time, derived by correlating query and response events with the same transaction_id within a 5-minute bucket. Only fully-paired queries contribute.</description>
      <chart>
        <search>
          <query>index=$index_filter$ sourcetype=routeros (eventtype=routeros_dns_query OR eventtype=routeros_dns_response) host=$host_filter$
| eval bucket=floor(_time/300)
| stats earliest(_time) as qt, latest(_time) as rt, count as events by host, transaction_id, bucket
| where events=2
| eval ms = (rt - qt) * 1000
| timechart avg(ms) as "Avg (ms)", perc95(ms) as "p95 (ms)"</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
        <option name="charting.chart">line</option>
        <option name="charting.legend.placement">right</option>
      </chart>
    </panel>
  </row>

  <row>
    <panel>
      <title>Top Queried Domains</title>
      <table>
        <search>
          <query>index=$index_filter$ sourcetype=routeros eventtype=routeros_dns_query host=$host_filter$ | top limit=20 query | rename query as Domain, count as Count, percent as Percent</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
      </table>
    </panel>
    <panel>
      <title>Top NXDOMAIN Domains</title>
      <description>Correlates queries and responses by transaction_id + 5-minute bucket. Adjust the bucket if your DNS traffic exceeds ~65k queries per host per 5 min.</description>
      <table>
        <search>
          <query>index=$index_filter$ sourcetype=routeros (eventtype=routeros_dns_query OR eventtype=routeros_dns_response) host=$host_filter$
| eval bucket=floor(_time/300)
| stats values(query) as query, values(reply_code) as reply_code by host transaction_id bucket
| where reply_code="NXDomain"
| top limit=20 query
| rename query as Domain, count as Count, percent as Percent</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
      </table>
    </panel>
  </row>

  <row>
    <panel>
      <title>Top Querying Clients</title>
      <description>Hostname comes from the DHCP lookup populated by the 'Update DHCP Hostname Lookup' scheduled search. Shows '-' if no DHCP mapping is known for that IP.</description>
      <table>
        <search>
          <query>index=$index_filter$ sourcetype=routeros eventtype=routeros_dns_query host=$host_filter$
| stats count by src, src_nt_host
| eval Hostname = if(isnull(src_nt_host) OR src_nt_host="", "-", src_nt_host)
| sort -count
| head 20
| rename src as IP, count as Count
| table Hostname IP Count</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
      </table>
    </panel>
    <panel>
      <title>Top Clients by NXDOMAIN Responses</title>
      <description>Surfaces clients generating the most non-existent-domain responses. Useful for spotting DGA-style malware, misconfigured devices, or stale bookmarks.</description>
      <table>
        <search>
          <query>index=$index_filter$ sourcetype=routeros (eventtype=routeros_dns_query OR eventtype=routeros_dns_response) host=$host_filter$
| eval bucket=floor(_time/300)
| stats values(reply_code) as reply_code, values(src) as src, values(src_nt_host) as src_nt_host by host transaction_id bucket
| where reply_code="NXDomain"
| stats count by src, src_nt_host
| eval Hostname = if(isnull(src_nt_host) OR src_nt_host="", "-", src_nt_host)
| sort -count
| head 20
| rename src as IP, count as "NXDOMAIN Count"
| table Hostname IP "NXDOMAIN Count"</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
      </table>
    </panel>
  </row>

  <row>
    <panel>
      <title>Newly Observed Domains (last 24h)</title>
      <description>Domains queried for the first time in the last 24 hours, compared against a 30-day baseline. Useful for spotting phishing first-contact, malware C2, or new device integrations. Search runs over a fixed 30-day window and ignores the time picker.</description>
      <table>
        <search>
          <query>index=$index_filter$ sourcetype=routeros eventtype=routeros_dns_query host=$host_filter$
| stats earliest(_time) as first_seen, count, values(src_nt_host) as clients by query
| where first_seen >= relative_time(now(), "-24h")
| eval "First Seen" = strftime(first_seen, "%Y-%m-%d %H:%M:%S")
| eval Clients = if(isnull(clients) OR clients="", "-", clients)
| sort - first_seen
| head 50
| rename query as Domain, count as Queries
| table Domain "First Seen" Queries Clients</query>
          <earliest>-30d</earliest>
          <latest>now</latest>
        </search>
      </table>
    </panel>
    <panel>
      <title>Long-Subdomain Queries (DNS tunneling indicator)</title>
      <description>Queries with domain length >= 100 characters. DNS tunneling tools (iodine, dnscat2) pack payload data into long subdomains. Normal hostnames are rarely above 50 characters.</description>
      <table>
        <search>
          <query>index=$index_filter$ sourcetype=routeros eventtype=routeros_dns_query host=$host_filter$
| eval qlen=len(query)
| where qlen >= 100
| stats count as Count, values(src_nt_host) as Hostname, values(src) as IP, max(qlen) as "Max Length" by query
| eval Hostname = if(isnull(Hostname) OR Hostname="", "-", Hostname)
| sort - "Max Length"
| head 20
| rename query as Domain
| table Domain "Max Length" Count Hostname IP</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
      </table>
    </panel>
  </row>

  <row>
    <panel>
      <title>Record Type Distribution</title>
      <chart>
        <search>
          <query>index=$index_filter$ sourcetype=routeros eventtype=routeros_dns_query host=$host_filter$ | stats count by record_type | sort -count</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
        <option name="charting.chart">pie</option>
      </chart>
    </panel>
  </row>
</dashboard>
