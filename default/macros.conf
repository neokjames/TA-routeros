## Default index containing RouterOS events. Override in local/macros.conf if
## your RouterOS data lives in a different index. Example local override:
##   [routeros_index]
##   definition = index=mikrotik
[routeros_index]
definition = index=network
iseval = 0
